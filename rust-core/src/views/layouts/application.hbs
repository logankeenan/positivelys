<!DOCTYPE html>
<html lang="en">
<head>
    <meta charset="utf-8">
    <meta name="viewport" content="width=device-width, initial-scale=1, shrink-to-fit=no">
    <title>Positivelys</title>
    <link rel="stylesheet" href="https://cdn.jsdelivr.net/npm/bootstrap@4.5.3/dist/css/bootstrap.min.css"
          integrity="sha384-TX8t27EcRE3e/ihU7zmQxVncDAy5uIKz4rEkgIXeMed4M0jlfIDPvg6uqKI2xXr2"
          crossorigin="anonymous">
    <style>
        .border-bottom-gray {
            border-bottom: 1px solid #E6E6E6;
        }
    </style>

</head>
<body>
<nav class="navbar bg-white sticky-top border-bottom-gray">
    <span class="font-weight-bold  navbar-brand mb-0">Positivelys</span>
</nav>
{{> page_content}}
<script src="https://unpkg.com/morphdom@2.6.1/dist/morphdom-umd.min.js"></script>
<script>
    document.addEventListener('submit', async function (event) {
        console.log('here');
        event.preventDefault();

        const formData = new FormData(event.target);
        const data = Object.fromEntries(formData.entries());
        const response = await fetch(event.target.action, {
            method: 'POST',
            redirect: 'follow',
            body: JSON.stringify(data)
        });
        const htmlResponse = await response.text();

        morphdom(document.documentElement, htmlResponse);

        history.pushState(undefined, undefined, response.url);
    });

    document.addEventListener('click', async function (event) {
        let anchorTag = event.target;
        if (anchorTag.tagName.toLowerCase() === "a") {
            event.preventDefault();

            const response = await fetch(anchorTag.href);

            const htmlResponse = await response.text();

            morphdom(document.documentElement, htmlResponse);

            history.pushState(undefined, undefined, anchorTag.href);
        }
    });

    window.addEventListener('popstate', async function (event) {
        let url = event.target.location.toString();

        const response = await fetch(url);

        const htmlResponse = await response.text();

        morphdom(document.documentElement, htmlResponse);

        history.pushState(undefined, undefined, url);
    });


</script>
</body>
</html>